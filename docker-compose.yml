version: '3.8'

services:
  qip-chat:
    container_name: qip-chat
    build: .
    ports:
      - "8505:8505"
    volumes:
      - .:/app
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Jakarta
